// Broad category a card belongs to — drives the card face's title color and card-type label
// (see Card.Category/CardGameObject.BuildCard). Troop is by far the most common, hence the
// default on Card itself.
public enum CardCategory
{
    Troop,
    Building,
    Spell,
}
