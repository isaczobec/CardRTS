// Broad category an upgrade belongs to — purely a shop-filtering concern (see
// UpgradeShopUIManager's Offense/Defense/Utility filter buttons), mirrors CardCategory's own
// role for the card shop's Troop/Building/Spell filter. Doesn't drive any gameplay behavior.
public enum UpgradeCategory
{
    Offense,
    Defense,
    Utility,
}
